# support ruby version: 2.2 2.3 2.4 2.5 2.6
# support rails on Ubuntu20.0x/CentOS7.x, not support Ubuntu18.04

ruby_version: "2.5"
ruby_bundle_version: "2.3.0"

# have a dedicated  ubuntu package
# https://github.com/rvm/rvm
# https://github.com/rvm/ubuntu_rvm
ruby_rvm:
  url: https://get.rvm.io
  download_path: /tmp/rvm-installer.sh

ruby_applications: []
  # - rails

ruby_applications_rails: 
  port: "3000"
  path: "/data/apps/rails"
  project: "myrails" 
